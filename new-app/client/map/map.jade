template(name='map')

  h1 Floorplan

  .floorplan
    .stage stage

    .tableBox
      each tables
        span.oneTable(id=name) {{name}}
        if this.spacer
          span.spacer

        if this.endcap
          br
      //- each rows
      //-   each cols
      //-     span.oneTable {{tableName this ..this}}
      //-   br
